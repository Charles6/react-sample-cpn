import React from 'react';
import cx from 'classnames';

function NewFeed() {
    return (
      <div className={cx('box', 'news-box')}>
        News Feed
      </div>
    );
  }
  
  export default NewFeed;